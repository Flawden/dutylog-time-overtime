# DutyLog: Time & Overtime

DutyLog: Time & Overtime — календарь смен, переработок, отгулов, задач, важных дней и уведомлений.

Текущий стек:

- Spring Boot 3.3.5
- Java 17
- Spring Web / Data JPA / Security / Validation
- H2 для локальной разработки
- PostgreSQL + Flyway для production-профиля
- простой веб-интерфейс на HTML/CSS/JS
- PWA-заготовка: manifest, service worker, иконки
- Docker Compose для запуска на сервере

## Идея продукта

Пользователь открывает календарь и отмечает, какая смена была в конкретный день. В базовом наборе есть три встроенные смены: **«Дневная»**, **«Ночная»** и **«Выходной»**. Всё остальное пользователь создаёт сам: «5 часов», «12 часов», «Сутки», «Отпуск», «Больничный» и любые другие варианты. Для каждой кастомной смены задаются название, цвет и количество часов.

В каждый день можно провалиться и написать заметку в Markdown. Если в дне есть заметка, на клетке календаря появляется отметка.

Также у дня теперь есть отдельные задачи с чекбоксами. Это не Markdown-задачи внутри заметки, а отдельные сущности: если есть невыполненные задачи, на клетке календаря появляется `!`; когда задачи выполнены, индикатор гаснет/становится спокойным.

Важные дни вынесены отдельно: дни рождения, годовщины, платежи, техосмотры и любые повторяющиеся события. Поддерживаются режимы: один раз, каждый месяц, каждый год. В календаре такие дни помечаются `★`.

От выбранного дня можно запустить автозаполнение графика: 2/2, день/ночь/48, пятидневка 5/2, день/72, ночь/72. По умолчанию график заполняется на 31 день вперёд, включая следующий месяц. Пятидневка привязана к реальным дням недели: Пн–Пт рабочие, Сб–Вс выходные. После заполнения каждый день всё равно редактируется вручную.

Добавлена полноценная бухгалтерия переработки и отгулов. Переработка теперь хранится не как месячная сумма, а как отдельные начисления: дата, время, часы и причина. Отгул списывается отдельной записью и автоматически распределяется по старым начислениям по FIFO — сначала самые старые остатки.

Переработку можно вводить вручную в часах или считать автоматически по интервалу: начало, конец, обед в минутах и плановые часы, которые нужно вычесть. Формула: `переработка = конец - начало - обед - плановые часы`. Поэтому ночной интервал `20:00–08:00` с обедом 60 минут даст `11 ч`, а сценарий “остался в ночь” `17:00–08:00` без вычета плана даст `15 ч`. Если интервал проходит через несколько дат, сервер раскладывает его на отдельные начисления и не даёт записать пересекающийся период второй раз.

Для частых ситуаций есть быстрые сценарии. Базово доступны `+2ч после смены`, `+4ч после смены`, `остался в ночь`, `смена + ночь`, `обычная смена целиком`, но теперь пользователь может создавать и свои сценарии: от начала или конца смены, до фиксированного времени, через N минут, с обедом из смены/своим/нулевым, с вычетом плана из смены/своим/нулевым. Сценарии только заполняют форму переработки, а перед начислением пользователь может поправить время, обед, план или причину.

Пример: 20 числа начислено `+2 ч`, 21 числа начислено `+3 ч`, а 23 числа списан отгул `-4 ч`. Система заберёт `2 ч` с 20 числа и `2 ч` с 21 числа. Остаток по 20 числу станет `0`, остаток по 21 числу — `1 ч`. Часы не сгорают при переходе между месяцами: переработку из мая можно списать в августе.




## Что добавлено в v20.3-diagnostics-settings-polish

- Добавлена вкладка-оглавление внутри `⚙`: Профиль, Время, Смены, Сценарии, Уведомления, Диагностика.
- Секция **Время и регион** стала понятнее: меньше лишних кнопок, основной сценарий — `Сохранить и применить к сменам`.
- Изменения дефолтов дневной/ночной смены автоматически сохраняются и применяются к встроенным сменам после короткой паузы.
- Добавлен блок **Диагностика** во вкладке `⚙`: версия frontend/backend, service worker, браузер, CSRF-cookie, серверное время, профиль Spring, база данных и Telegram-настройки.
- Добавлен endpoint `GET /api/system/status` для web-safe диагностики текущего аккаунта и сервера. Секреты не отдаются.
- Добавлен `RequestDiagnosticsFilter`: в логах появляются метод, путь, HTTP-статус, длительность и `X-Request-Id`.
- Добавлен `docs/GIT_WORKFLOW.md` с безопасным сценарием Git-версий и откатов.
- Frontend-кэш поднят до `v20.3`.


## Что исправлено в v20.2.1-header-avatar-fix

- Исправлен баг шапки: аватар профиля больше не прилипает к стрелке месяца на узких экранах.
- На мобильной ширине блок пользователя прижимается вправо, а имя аккуратно обрезается, если не хватает места.
- Frontend-кэш поднят до `v20.2.1`.

## Что добавлено в v20.2-telegram-quick-actions

- Telegram-бот теперь умеет менять данные, а не только читать их.
- Добавлены быстрые команды для задач: `/task текст`, `/task завтра текст`, `/done 12`.
- `/tasks` теперь показывает id задач, чтобы их можно было закрывать из Telegram.
- Добавлены быстрые команды переработок: `/ppr 17-08 причина` для интервала и `/ppr 2 причина` для ручного начисления часов.
- Добавлено списание отгула через `/timeoff 8 причина` или `/отгул 8 причина`; списание идёт через существующий FIFO-механизм.
- Быстрые команды принимают дату первым аргументом: `2026-07-10`, `10.07`, `сегодня`, `завтра`.
- Для интервала переработки можно указывать `обед60` и `план0/план8`, например `/ppr 10.07 17-08 обед60 план0 ППР после смены`.

## Что добавлено в v20.1-telegram-notifications

- Telegram-бот теперь может сам присылать напоминания: о сменах, задачах, важных днях и вечерний дайджест.
- Напоминания считаются тем же `NotificationService`, что и для веба/будущего Android, поэтому правила не дублируются.
- В блоке **Telegram-бот** появился переключатель `присылать напоминания в Telegram`.
- Чтобы не слать одно и то же много раз, добавлен журнал доставок `telegram_notification_deliveries`.
- Глобально отправку можно включать/выключать переменной `DUTYLOG_TELEGRAM_NOTIFICATIONS_ENABLED`; интервал скана настраивается через `DUTYLOG_TELEGRAM_NOTIFICATION_SCAN_DELAY_MS`.

## Что добавлено в v20.0-telegram-foundation

- Telegram-бот добавлен прямо в текущий backend DutyLog, отдельный сервис пока не нужен.
- В профиле во вкладке `⚙` появился блок **Telegram-бот**.
- Подключение сделано безопасно через одноразовый код: в приложении создаётся `DL-123456`, затем в Telegram отправляется `/start DL-123456`.
- Добавлены первые read-only команды: `/today`, `/tomorrow`, `/week`, `/tasks`, `/balance`, `/help` и русские алиасы.
- Бот умеет показывать смену на сегодня/завтра, открытые задачи, важные дни и остаток переработок.
- Для production добавлены env-переменные `DUTYLOG_TELEGRAM_ENABLED`, `DUTYLOG_TELEGRAM_BOT_TOKEN`, `DUTYLOG_TELEGRAM_BOT_USERNAME`, `DUTYLOG_TELEGRAM_POLLING_ENABLED`.
- Long polling выключен по умолчанию. Чтобы бот реально начал читать сообщения, надо создать бота у `@BotFather`, задать токен и включить polling.

## Что добавлено в v19.4-time-region

- Во вкладке `⚙` появилась секция **Время и регион**.
- Можно указать рабочий регион/объект, рабочий часовой пояс и сдвиг от Москвы как понятную пометку.
- Интерфейс показывает текущее время в рабочем часовом поясе и в часовом поясе браузера, чтобы не путаться при поездках или работе в другом регионе.
- Добавлены дефолты для дневной и ночной смены: начало, конец, обед и плановые часы.
- Дефолты можно применить к встроенным сменам `Дневная` и `Ночная`. После этого быстрые сценарии, уведомления и кнопка `план по смене` используют уже обновлённые параметры смен.
- Backend не менялся: настройки региона хранятся локально в браузере, а применение к сменам идёт через уже существующий `PATCH /api/shift-types/{id}`.

## Что добавлено в v19.1-settings-polish

- Интерфейс с вкладками из v19 сохранён, backend не менялся.
- Настройки стали отдельными секциями: время/регион, смены, быстрые сценарии и уведомления.
- Панель выбранного дня стала легче: в ней остались действия по конкретному дню, а создание/настройка сущностей переехала в `⚙`.
- Создание смен и настройка встроенных/кастомных смен выполняется во вкладке `⚙`.
- Пользовательские быстрые сценарии создаются во вкладке `⚙`, а применяются в блоке переработки выбранного дня.
- Важные дни снова добавляются в панели выбранного дня: ткнул дату — настроил событие прямо там.

## Что добавлено в v17.2-quick-scenario-customization

- Дневная смена по умолчанию теперь `08:30–17:00`, обед `30 мин`, план `8 ч`.
- Старый дефолт `06:30–17:00` мягко переносится на `08:30–17:00`, но только если пользователь не менял время сам.
- Быстрые сценарии стали адаптивными и больше не выезжают за правую панель.
- Добавлена система пользовательских быстрых сценариев через `/api/quick-scenarios`.
- В веб-интерфейсе можно создать свой сценарий: старт от начала/конца смены, конец через N минут/в указанное время/в конец смены, обед, план и причина по умолчанию.

## Что добавлено в v15.1-quick-scenarios

Эта версия добавляет быстрые сценарии для переработки:

- `+2ч после смены` и `+4ч после смены` — быстро создать переработку сразу после окончания выбранной смены;
- `остался в ночь` — заполнить интервал от конца дневной смены до 08:00 следующего дня;
- `с начала смены + ночь` — занести интервал от начала смены до утра следующего дня с вычетом плана выбранной смены;
- `обычная смена целиком` — подставить время выбранной смены, обед и плановые часы для проверки или ручной корректировки;
- сценарии не начисляют часы сразу, а только заполняют форму, чтобы перед сохранением можно было поправить детали.

## Что добавлено в v15.0-shift-time-model

- У смен появились время начала, время конца, обед по умолчанию и плановые часы.
- Встроенные смены получили базовые настройки: дневная `08:30–17:00`, ночная `20:00–08:00`, выходной `0 ч`.
- Кнопки `план по смене`, `время по смене` и `по смене` теперь используют плановые часы и параметры выбранной смены.

## Что добавлено в v14.5-ledger-polish

Эта версия полирует журнал переработок:

- фильтр по периоду `с/по`;
- быстрые кнопки `этот месяц` и `всё время`;
- фильтр по статусу начисления: все, с остатком, частично списанные, полностью списанные;
- поиск по причине, дате, времени и списаниям;
- итоги по отфильтрованной таблице;
- бейджи состояния начислений;
- подсветка выбранного дня в таблице;
- подтверждение удаления начислений и списаний.

## Что добавлено в v14.4-overtime-split-and-duplicates

- Рассчитанная переработка через интервал времени теперь раскладывается на несколько начислений, если интервал проходит через разные даты.
- Обычные ночные интервалы режутся по датам, поэтому `17:00 → 08:00` попадёт двумя строками: до полуночи и после полуночи.
- Ровные сутки вида `08:00 → 08:00` следующего дня режутся пополам, чтобы календарь не показывал все часы только на дате начала.
- Нельзя дважды начислить один и тот же рассчитанный период или пересекающийся интервал: backend вернёт понятную ошибку.
- Ручные начисления без начала/конца остались без изменений.

## Что добавлено в v14.3-overtime-date-fix

- Исправлено начисление переработки через интервал времени: дата записи теперь берётся из поля `Начало`, а не из текущего открытого дня календаря.
- Если открыть 9 июля, но указать начало `3 июля 08:30`, переработка попадёт на 3 июля.
- Для ручного начисления без интервала всё работает как раньше: запись создаётся на выбранный день.

## Что добавлено в v14.2-overtime-time-calc

Эта версия добавляет автоподсчёт переработки по времени поверх версии с аккордеоном:

- поля начала и конца переработки/фактической работы;
- поле `обед, мин`, которое вычитается из расчёта;
- поле `вычесть план, ч`, если вводится вся фактическая смена целиком;
- кнопка `план по смене`, которая подставляет часы выбранной смены;
- кнопка `17–08` для быстрого сценария “остался в ночь”;
- ручной ввод часов сохранён;
- backend пересчитывает часы сам и хранит параметры расчёта;
- добавлена миграция `V5__overtime_time_calculation.sql`.

## Что добавлено в v13-overtime-accounting

Эта версия заменяет идею “месячного баланса” нормальной таблицей начислений и списаний:

- добавлены начисления переработки: день, диапазон времени, часы, причина;
- добавлены списания отгулов: день, часы, причина;
- списания автоматически распределяются по FIFO со старых начислений;
- в календаре показывается общий остаток переработки, а не только баланс текущего месяца;
- добавлена таблица переработок с колонками: день, время, начислено, причина, использовано, куда списано, остаток;
- добавлены endpoint’ы `/api/overtime/account`, `/api/overtime/credits`, `/api/overtime/usages`;
- добавлена миграция `V4__overtime_accounting.sql`.

## Что добавлено в v12-android-ready

Эта версия добавляет слой, который нужен для настоящего Android-клиента:

- мобильная авторизация через `Authorization: Bearer <accessToken>`;
- refresh token с ротацией;
- токены в базе хранятся только как SHA-256 хэши;
- веб-версия продолжает работать через `JSESSIONID`;
- новый endpoint `GET /api/mobile/bootstrap?from=&to=` — одним запросом отдаёт профиль, календарь, задачи, важные дни и баланс;
- новый endpoint `POST /api/mobile/sync` — пакетная синхронизация изменений дней из offline-очереди Android;
- endpoint’ы управления мобильными сессиями;
- добавлена миграция `V3__mobile_auth_tokens.sql`;
- обновлена Android-документация.

## Что добавлено в v11-important-days-tasks

Эта версия добавляет поверх v10 новый продуктовый слой:

- отдельные задачи дня с чекбоксами;
- индикатор `!` в календаре, пока есть невыполненные задачи;
- отдельные важные дни;
- повторения важных дней: один раз, каждый месяц, каждый год;
- индикатор `★` на календаре для важных дней;
- новые сущности `DayTask` и `ImportantDay`;
- новые endpoint’ы `/api/tasks` и `/api/important-days`;
- `/api/calendar?from=&to=` теперь отдаёт ещё `tasks` и `importantDays`;
- добавлена миграция `V2__important_days_and_tasks.sql`.

## Что добавлено в v9-production-foundation

Эта версия — первый шаг от MVP к нормальному продукту:

- добавлен PostgreSQL-драйвер;
- добавлен Flyway;
- добавлена production-миграция `V1__init.sql`;
- добавлен production-профиль `application-prod.properties`;
- в production отключён `ddl-auto=update`, используется `ddl-auto=validate`;
- H2 оставлена только для локального dev-режима;
- добавлен Dockerfile;
- добавлен `docker-compose.yml` с PostgreSQL и приложением;
- добавлен `.env.example`;
- добавлен пример nginx-конфига;
- добавлен скрипт бэкапа PostgreSQL;
- добавлен Spring Boot Actuator health endpoint;
- подготовлены документы `docs/ROADMAP.md` и `docs/ANDROID_API_PLAN.md`.

## Быстрый старт для разработки

Нужны: **JDK 17+** и **Maven**. Через IntelliJ IDEA можно просто открыть папку с `pom.xml`.

### Через IntelliJ IDEA

1. File → Open → выбери папку проекта.
2. Дождись загрузки Maven-зависимостей.
3. Запусти `ShiftsApplication`.
4. Открой http://localhost:8080

### Через консоль

```bash
mvn spring-boot:run
```

В dev-режиме используется H2. База лежит в:

```text
./data/shifts.mv.db
```

Консоль БД:

```text
http://localhost:8080/h2-console
JDBC URL: jdbc:h2:file:./data/shifts
User: sa
Password: пустой
```

## Запуск production-режима через Docker Compose

1. Скопируй `.env.example` в `.env`:

```bash
cp .env.example .env
```

2. Открой `.env` и поменяй пароль:

```text
POSTGRES_PASSWORD=change_me_strong_password
SPRING_DATASOURCE_PASSWORD=change_me_strong_password
```

3. Запусти:

```bash
docker compose up -d --build
```

4. Открой:

```text
http://localhost:8080
```

5. Проверка health endpoint:

```text
http://localhost:8080/actuator/health
```

В production-режиме таблицы создаёт Flyway из файла:

```text
src/main/resources/db/migration/postgresql/V1__init.sql
```

Hibernate только проверяет, что схема БД соответствует сущностям.

## Запуск production-режима без Docker

Нужен установленный PostgreSQL. Создай базу и пользователя, затем задай переменные окружения:

```bash
export SPRING_PROFILES_ACTIVE=prod
export SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/shift_calendar
export SPRING_DATASOURCE_USERNAME=shift_calendar
export SPRING_DATASOURCE_PASSWORD=your_password
mvn spring-boot:run
```

## Бэкап базы

Если проект запущен через Docker Compose:

```bash
./deploy/scripts/backup-postgres.sh
```

Бэкап появится в папке:

```text
./backups/
```

## Пример nginx

Шаблон лежит здесь:

```text
deploy/nginx/shift-calendar.conf.example
```

Для реального сервера:

1. подставь свой домен;
2. прокинь домен на сервер;
3. поставь nginx;
4. добавь конфиг;
5. выпусти HTTPS через certbot.

## Авторизация

Теперь есть две схемы авторизации:

```text
Веб:     JSESSIONID cookie после обычной формы входа
Android: Authorization: Bearer <accessToken>
```

Мобильные endpoint’ы:

```text
POST   /api/mobile/auth/login
POST   /api/mobile/auth/refresh
POST   /api/mobile/auth/logout
GET    /api/mobile/auth/me
GET    /api/mobile/auth/sessions
DELETE /api/mobile/auth/sessions/{id}
```

Для браузерного профиля используются CSRF-защищённые web endpoint’ы:

```text
GET    /api/profile/sessions
DELETE /api/profile/sessions/{id}
```

Access token короткий, refresh token длиннее. При обновлении refresh token ротируется: старая пара заменяется новой. В базе хранятся не сами токены, а SHA-256 хэши.

Пароли хранятся через BCrypt. Данные пользователей изолированы: каждый запрос берёт текущего пользователя из сессии или Bearer-токена и работает только с его сменами/днями.

CSRF для web-интерфейса включён: Spring кладёт токен в cookie `XSRF-TOKEN`, а frontend отправляет его в заголовке `X-XSRF-TOKEN` на изменяющих запросах. Мобильный API `/api/mobile/**` остаётся stateless под `Authorization: Bearer ...` и исключён из CSRF.

## Что внутри

```text
src/main/java/ru/daniil/shifts/
├── ShiftsApplication.java
├── config/SecurityConfig.java
├── dto/Dtos.java
├── model/
│   ├── AppUser.java
│   ├── ShiftType.java
│   ├── DayEntry.java
│   ├── DayTask.java
│   ├── ImportantDay.java
│   ├── MobileAuthToken.java
│   ├── OvertimeCredit.java
│   ├── OvertimeUsage.java
│   ├── OvertimeAllocation.java
│   └── RepeatMode.java
├── repo/
│   ├── UserRepository.java
│   ├── ShiftTypeRepository.java
│   ├── DayEntryRepository.java
│   ├── DayTaskRepository.java
│   └── ImportantDayRepository.java
├── service/
│   ├── CurrentUserService.java
│   ├── ShiftTypeService.java
│   ├── DayEntryService.java
│   ├── CalendarService.java
│   ├── OvertimeService.java
│   ├── TaskService.java
│   ├── ImportantDayService.java
│   ├── MobileAuthService.java
│   └── exception/ApiException.java
└── web/
    ├── ApiExceptionHandler.java
    ├── AuthController.java
    ├── ShiftTypeController.java
    ├── DayController.java
    ├── CalendarController.java
    ├── OvertimeController.java
    ├── TaskController.java
    └── ImportantDayController.java

src/main/resources/
├── application.properties
├── application-prod.properties
├── db/migration/postgresql/V1__init.sql
├── db/migration/postgresql/V2__important_days_and_tasks.sql
├── db/migration/postgresql/V3__mobile_auth_tokens.sql
└── static/
    ├── index.html
    ├── login.html
    ├── manifest.json
    ├── service-worker.js
    └── icons/

deploy/
├── nginx/shift-calendar.conf.example
└── scripts/backup-postgres.sh
```


## v15.0-shift-time-model

В v15 тип смены стал умнее: помимо названия, цвета и часов у него есть время начала, время конца, обед по умолчанию и плановые часы. Это нужно для более точной переработки, будущих уведомлений и Android-будильника.

Пример смены:

```json
{
  "name": "Дневная",
  "hours": 8,
  "startTime": "06:30",
  "endTime": "17:00",
  "breakMinutes": 30,
  "plannedHours": 8
}
```

## v11-important-days-tasks

В v11 добавлены задачи дня и важные дни. Это отдельные сущности, поэтому Android и Telegram потом смогут работать с ними напрямую: показывать список задач, чекать выполнение, присылать уведомления по важным датам.

## v10-api-architecture

В v10 бизнес-логика вынесена из контроллеров в сервисы, а для Android/PWA добавлен диапазонный календарный endpoint. Старые endpoint’ы веб-версии сохранены, поэтому текущий интерфейс продолжает работать.

## API

Все эндпоинты, кроме регистрации и health, требуют залогиненной сессии.

| Метод | URL | Что делает |
|---|---|---|
| POST | `/api/auth/register` | регистрация |
| GET | `/api/auth/me` | текущий пользователь |
| POST | `/perform_login` | вход через Spring Security |
| POST | `/logout` | выход |
| GET | `/api/shift-types` | список типов смен |
| POST | `/api/shift-types` | создать тип смены |
| PATCH | `/api/shift-types/{id}` | настроить тип смены |
| DELETE | `/api/shift-types/{id}` | удалить пользовательский тип |
| GET | `/api/calendar?from=2026-06-01&to=2026-07-31` | Android-friendly календарь диапазоном: смены + дни + баланс |
| GET | `/api/days?year=2026&month=7` | записи месяца для текущего веба |
| PUT | `/api/days/2026-07-02` | сохранить день |
| POST | `/api/days/fill` | заполнить график |
| GET | `/api/overtime/balance?from=&to=` | сводка переработок и отгулов |
| GET | `/api/overtime/ledger?from=&to=` | журнал переработок/отгулов |
| GET | `/actuator/health` | health check |

Пример сохранения дня:

```json
{
  "shiftTypeId": 1,
  "note": "# ППР\nОстался после смены.",
  "overtimeHours": 7,
  "timeOffHours": 0
}
```

## Важные замечания

- Старую H2-базу нельзя автоматически перенести в PostgreSQL этой версией. Для теста production-режима проще начать с чистой базы.
- Если уже запускал старые версии в dev-режиме, а поведение странное — удали папку `data` и зарегистрируй пользователя заново.
- Production-режим уже не использует `ddl-auto=update`; все будущие изменения БД надо делать через новые файлы Flyway: `V2__...sql`, `V3__...sql` и так далее.
- Android-приложение лучше делать после стабилизации API и авторизации через токены.

## Следующие шаги

Смотри:

```text
docs/ROADMAP.md
docs/ANDROID_API_PLAN.md
docs/API.md
```


## Экспорт журнала переработок

В таблице переработок доступны кнопки `CSV` и `Excel`. Выгрузка учитывает текущие фильтры таблицы: период, статус начислений и поиск.

Также доступны прямые endpoint’ы:

```text
GET /api/overtime/export.csv
GET /api/overtime/export.xls
```

В отчёт попадают дата переработки, интервал, начисленные часы, причина, использованные часы, куда списано, остаток, обед, вычтенный план и признак автоматического расчёта.

### v15.2: быстрые сценарии как карточки

Блок быстрых сценариев переработки стал более понятным: вместо набора коротких кнопок теперь отображаются карточки с названием, назначением и кратким описанием. Интерфейс показывает, какая смена выбрана в текущем дне, её время, плановые часы и обед. Если у смены не настроено время начала/окончания, неподходящие сценарии блокируются.

Сценарий только заполняет форму переработки. Перед нажатием «Начислить» пользователь всё ещё может вручную поправить интервал, обед, вычтенный план или причину.

### Уведомления и напоминания

В версии v17 добавлен первый слой уведомлений. Backend рассчитывает напоминания по настройкам пользователя и отдаёт их через API. Сейчас веб-версия показывает список ближайших напоминаний и может запросить разрешение браузера на Notification API. Надёжные будильники и локальные уведомления, которые будут работать даже после закрытия приложения, логичнее делать уже в Android-клиенте.

Новые API:

```text
GET   /api/notifications/settings
PATCH /api/notifications/settings
GET   /api/notifications/upcoming?from=2026-07-01&to=2026-07-31
```

`/api/calendar?from=&to=` также возвращает `notificationSettings` и `reminders`.

### v17.1: полировка уведомлений

Уведомления теперь можно тоньше настраивать: у конкретной смены можно отключить напоминания или задать своё время, например ночную напоминать за 120 минут, а дневную за 60 минут. В веб-интерфейсе добавлены быстрые пресеты времени и кнопка проверки напоминаний на завтра.

### v18.0: задачи 2.0

Задачи теперь стали отдельным полноценным модулем. У задачи можно указать категорию, приоритет, срок выполнения, время срока и индивидуальное напоминание. В календаре просроченные задачи помечаются отдельным красным индикатором `!!`, а внутри дня задачи можно фильтровать по статусу и категории.

Поля задачи:

- дата задачи;
- текст;
- категория;
- приоритет: низкий / обычный / важный / срочный;
- срок выполнения;
- время срока;
- индивидуальное напоминание;
- статус выполнения.

Это подготовка под будущий отдельный экран задач в Android и Telegram-команды по задачам.

## v19.2 — важные дни обратно в панель дня

- Важные дни возвращены в панель выбранного дня: теперь можно ткнуть дату и сразу добавить день рождения/платёж/событие.
- Из вкладки ⚙ убрана секция важных дней, чтобы настройки не выглядели странно и не дублировали дневной контекст.
- Исправлено отображение скрытых вкладок: настройки больше не появляются под календарём из-за CSS `display:flex`.
### v19.3 — мобильная полировка

В v19.3 улучшен мобильный UX: вкладки становятся нижней навигацией, панель выбранного дня открывается как нижняя шторка, кнопки и поля стали крупнее под палец, а формы и фильтры лучше адаптируются к узкому экрану. Backend не менялся.




## v19.10.2 — branding

- Приложение переименовано в **DutyLog: Time & Overtime**.
- Обновлены заголовки страниц, экран входа, PWA manifest, тестовое уведомление и документация.
- `app.css` и `app.js` подключаются с `?v=19.10.2`.
- Service worker получил cache name `dutylog-shell-v19.10.2`.
- Технические имена БД/пакетов и endpoint’ов сохранены, чтобы не ломать существующие данные и деплой.

## v19.10.1 — cleanup после слияния

- Архив в v19.10.1 был приведён к структуре `v19.10.1/shift-calendar`.
- `CHANGES.md` дополнен релизами `v19.5–v19.10`.
- Уточнена документация по CSRF: web-cookie интерфейс защищён, mobile Bearer API остаётся stateless.
- `app.css`/`app.js` подключаются с `?v=19.10.1`, а service worker отдаёт JS/CSS по network-first.
- UI профиля больше не ходит в `/api/mobile/auth/sessions`; для браузера добавлены CSRF-защищённые `/api/profile/sessions`.

## v19.5 — слияние веток: безопасность и тесты из v20

Из потерянной ветки v20 перенесено: CSRF-защита веба (cookie XSRF-TOKEN + заголовок X-XSRF-TOKEN, мобильный API исключён как stateless), fail-hard пароли в docker-compose (без .env не стартует), 7 тестов OvertimeService (`mvn test`). Если ловишь 403 на POST после обновления — почисти cookies и перезайди. Аварийный откат CSRF: `.csrf(csrf -> csrf.disable())` в SecurityConfig.

Правило: трогаешь OvertimeService — прогоняешь тесты до и после.


## v19.10 — Профиль

Вкладка ⚙, первая карточка: отображаемое имя (показывается в шапке с аватаром-инициалами), день рождения (в этот день календарь поздравляет баннером), смена пароля (требует текущий; после смены все мобильные сессии отзываются), список активных устройств с кнопкой отзыва — UI поверх уже существовавшего MobileAuthService.

Принцип полей профиля: каждое поле читает какая-то фича. Хочешь добавить поле — сначала назови фичу.
Миграция: V11 (display_name, birthday — nullable). В dev H2 колонки добавятся сами (ddl-auto=update).
