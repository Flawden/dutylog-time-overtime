# --- Production-профиль для сервера ---
# Запуск локально с PostgreSQL:
#   SPRING_PROFILES_ACTIVE=prod mvn spring-boot:run
# Запуск через Docker Compose см. docker-compose.yml и README.md.

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/shift_calendar}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:shift_calendar}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:change_me}
spring.datasource.driver-class-name=org.postgresql.Driver

# В production структуру БД меняют только миграции Flyway.
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration/postgresql
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

# H2-console нельзя оставлять открытой на публичном сервере.
spring.h2.console.enabled=false

# Если приложение стоит за nginx/прокси, Spring корректнее понимает HTTPS-заголовки.
server.forward-headers-strategy=framework

# Не светим технические детали ошибок наружу.
server.error.include-message=never
server.error.include-stacktrace=never
server.error.include-binding-errors=never

management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true

# Telegram bot. For the first production version long polling is enough.
# Create a bot via @BotFather, set DUTYLOG_TELEGRAM_ENABLED=true,
# DUTYLOG_TELEGRAM_BOT_TOKEN and DUTYLOG_TELEGRAM_POLLING_ENABLED=true.
dutylog.telegram.enabled=${DUTYLOG_TELEGRAM_ENABLED:false}
dutylog.telegram.bot-token=${DUTYLOG_TELEGRAM_BOT_TOKEN:}
dutylog.telegram.bot-username=${DUTYLOG_TELEGRAM_BOT_USERNAME:}
dutylog.telegram.polling-enabled=${DUTYLOG_TELEGRAM_POLLING_ENABLED:false}
dutylog.telegram.polling-delay-ms=${DUTYLOG_TELEGRAM_POLLING_DELAY_MS:3000}
