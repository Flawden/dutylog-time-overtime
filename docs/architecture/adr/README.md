# DutyLog architecture decision records

ADRs are immutable decision records. Accepted records are never silently rewritten: a new ADR supersedes an old one and the index links both. Any new cross-domain global store, generic runtime bridge, direct legacy state/DOM access or bypass of the generated API contract requires an ADR before implementation.

Statuses: `proposed`, `accepted`, `superseded`, `rejected`.
